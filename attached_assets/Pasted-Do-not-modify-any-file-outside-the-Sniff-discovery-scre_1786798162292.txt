Do not modify any file outside the Sniff/discovery screen's header component (mobile/app/(tabs)/discovery.tsx, or wherever the species chip row / chipScroll and Fresh/Popular sort control are defined — per the recent investigation, look for chipSortBand, chipScroll, and sortControl). Do not touch the chip scroll behavior, the sort control's positioning, or any layout/logic beyond adding the visual fade described below.

CONTEXT: The species chip row scrolls horizontally underneath the absolutely-positioned Fresh/Popular sort control. Per a recent investigation, chips currently get hard-clipped by the sort control's opaque background as they scroll underneath it — an abrupt cut-off rather than a soft fade.

FIX: Add a gradient fade effect on the right edge of the chip scroll area, where it meets the Fresh/Popular sort control, so chips appear to softly fade out rather than being abruptly clipped. Use whatever gradient/fade approach is idiomatic for this codebase (check if expo-linear-gradient or a similar library is already used elsewhere in the app for gradient effects, and match that pattern rather than introducing a new dependency if an existing one covers this). The fade should be a thin overlay (roughly matching the width of the existing SORT_CTRL_WIDTH padding already reserved at the end of the scroll content) positioned over the chip scroll area, transitioning from transparent to the screen's background color, sitting visually behind the sort control itself (the sort control's own opaque background can remain as the final hard boundary — the fade just softens the transition leading up to it, rather than replacing the sort control's masking entirely).

Do not change: the ScrollView's horizontal scroll behavior, the sort control's absolute positioning, the SORT_CTRL_WIDTH padding reservation, or any chip rendering/data logic.

ACCEPTANCE TEST: (1) Scrolling the species chip row shows chips softly fading out as they approach the Fresh/Popular control, rather than abruptly cutting off. (2) Fresh/Popular remains fully visible, fixed, and functional exactly as before. (3) Chip scrolling, tapping, and selection behavior are all completely unchanged. (4) tsc clean. (5) Visually test with the current small number of species chips (should look fine / fade barely noticeable with few chips) as well as verify the fade appears correctly if you can simulate/mock additional chips for testing purposes.