Do not modify any file outside api-server/src/routes/index.ts. This is a one-line remount fix.

CONTEXT: adminRouter is currently mounted AFTER the global requireClerkAuth middleware, which means GET /admin/cron/purge (intended to be called by an external scheduler carrying only the X-Purge-Secret header, no Clerk auth) gets rejected with 401 by requireClerkAuth before the purge-secret check inside admin.ts ever runs. This means the cron route's documented "external scheduler, secret-only" contract has never actually worked.

FIX: Remount adminRouter (or just the /admin/cron/purge route specifically, whichever is cleaner given how routes/index.ts is currently structured) BEFORE the global requireClerkAuth middleware, so the purge-secret check inside admin.ts is what gates this specific route, not Clerk auth. 

IMPORTANT: Every OTHER route under /admin (user management, reports, moderation, feedback, invites, quotas, dashboard) must remain fully protected by requireClerkAuth exactly as before — only the /admin/cron/purge route itself should be reachable without a Clerk session. If adminRouter as a whole is remounted before the auth middleware, you'll need to add requireClerkAuth (or ensure it's still applied) to every other route within adminRouter individually, OR — likely cleaner — extract just the /admin/cron/purge route into its own small router/handler mounted before requireClerkAuth, leaving the rest of adminRouter mounted after it exactly as today. Use whichever approach keeps every other admin route's protection unchanged with the least risk of accidentally exposing something.

ACCEPTANCE TEST: (1) A request to GET /admin/cron/purge with a valid X-Purge-Secret header and NO Clerk auth token succeeds. (2) A request to GET /admin/cron/purge with a missing or invalid X-Purge-Secret still rejects (401/403 as before), regardless of Clerk auth status. (3) Every other /admin/* route still requires valid Clerk auth exactly as before — verify by confirming a request to any other admin route without Clerk auth still gets rejected. (4) tsc clean.