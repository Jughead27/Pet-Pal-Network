Do not modify any file outside: mobile/components/HatchlingIcon.tsx, mobile/components/SniffIcon.tsx, mobile/components/PawIcon.tsx, mobile/components/KeyboardAwareScrollViewCompat.tsx (for deletion), api-server/src/routes/admin.ts (for the purge-secret fix), and api-server/src/middlewares/requireClerkAuth.ts (for the fail-open fix). Do not touch any other file.

PART 1 — DEAD CODE DELETION:
Before deleting, re-confirm via a repo-wide grep/search that none of these four files are imported or referenced anywhere: mobile/components/HatchlingIcon.tsx, mobile/components/SniffIcon.tsx, mobile/components/PawIcon.tsx, mobile/components/KeyboardAwareScrollViewCompat.tsx. If confirmed unused, delete all four files entirely. If any ARE referenced somewhere (double-check before trusting the prior audit), do not delete that one — report it instead and leave it in place.

PART 2 — TIMING-SAFE SECRET COMPARISON:
In api-server/src/routes/admin.ts around lines 1481-1489, the external purge endpoint currently compares the X-Purge-Secret header against the expected secret using plain string equality (===). Replace this with a timing-safe comparison (e.g. Node's built-in crypto.timingSafeEqual, following whatever pattern is idiomatic for this codebase — check if a timing-safe comparison utility already exists elsewhere in the codebase first and reuse it if so). Note: timingSafeEqual requires both buffers to be the same length, so handle the case where the provided secret has a different length than the expected one (return false/reject rather than throwing, and without leaking length information via a different response time — a common safe pattern is to first compare against a fixed-length hash of both values, or pad/hash before comparing). Preserve the exact existing behavior for a CORRECT secret (still authorizes normally) and an INCORRECT secret (still rejects) — only the comparison method changes, not the endpoint's logic or response.

PART 3 — AUTH MIDDLEWARE FAIL-OPEN FIX:
In api-server/src/middlewares/requireClerkAuth.ts around lines 105-110, if local user provisioning/lookup fails, the middleware currently logs the failure but still calls next() with the Clerk userId and a default member role — meaning a DB failure during this lookup lets a request proceed into protected routes without the suspended/deleted/local-user checks that would normally apply. Fix this to fail CLOSED instead: if local user provisioning/lookup fails, return an appropriate error response (e.g. 503 Service Unavailable, since this is a transient infrastructure failure, not an auth failure) instead of calling next(). Preserve all existing behavior for the successful-lookup path — only change what happens when the lookup itself fails.

ACCEPTANCE TEST: (1) App builds and runs normally — no broken imports from the deleted files. (2) The admin purge endpoint still works correctly with a valid secret and still rejects an invalid one. (3) Normal login/auth flow is completely unaffected — this fix only changes behavior during an actual DB failure during provisioning, which should be rare/nonexistent in normal testing. (4) tsc clean.