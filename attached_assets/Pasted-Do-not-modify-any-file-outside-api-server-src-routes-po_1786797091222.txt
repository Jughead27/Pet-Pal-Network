Do not modify any file outside api-server/src/routes/posts.ts. Do not touch any other route, any deletion logic beyond the specific error-handling path described, or any other endpoint's error handling.

CONTEXT: In the post deletion handler (around lines 550-553), when the R2 storage deletion fails, the code currently logs it via console.error but still returns a success response to the client — the failure is silently swallowed. This is inconsistent with how other routes in this codebase handle errors, which use a structured logger and return appropriate error responses rather than console.error + silent success.

FIX: 
1. Identify the existing structured logging pattern already used elsewhere in this codebase for error handling (check other routes — blocks.ts, reports.ts, users.ts were flagged as using structured logger errors correctly — find and match that exact pattern/logger).
2. Replace the console.error call for the R2 deletion failure with the same structured logger pattern used elsewhere.
3. Decide the correct response behavior for this case with care: the POST record itself may have already been successfully deleted from the database by this point (R2 is the media storage, separate from the DB row) — so a full failure response could be misleading if the DB deletion genuinely succeeded and only the associated media cleanup failed. Investigate the actual order of operations in this handler (does DB deletion happen before or after the R2 deletion attempt?) before deciding whether this should be: (a) still a success response since the user's post is gone, but now with proper structured logging so the orphaned R2 object is visible/trackable for cleanup, or (b) something else — use judgment based on what actually happened, and clearly document your reasoning for whichever choice you make in a code comment at that point.
4. Do not change the actual deletion logic itself (order of operations, what gets deleted, retry behavior) — only the error handling/logging/response behavior for the R2-failure case.

ACCEPTANCE TEST: (1) Deleting a post under normal conditions (R2 succeeds) works exactly as before — no behavior change. (2) If R2 deletion fails (which may be hard to trigger for a live test — code review of the logic change may be sufficient here rather than forcing a failure), the failure is now logged via the structured logger instead of console.error, and the response behavior matches whatever was decided in step 3 above with clear reasoning documented. (3) tsc clean. (4) No other route or endpoint's error handling is touched.