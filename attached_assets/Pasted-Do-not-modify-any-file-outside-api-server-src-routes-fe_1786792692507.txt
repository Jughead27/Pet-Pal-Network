Do not modify any file outside api-server/src/routes/feed.ts (or wherever the breed list for the Sniff filter is currently queried/returned — check for a dedicated endpoint or whether it's derived client-side) and, if needed, the Sniff/discovery mobile screen (app/(tabs)/discovery.tsx) that renders the breed dropdown. Do not touch species filtering logic, sort logic, or anything else — this is scoped to the breed list only.

CONTEXT: Sniff currently shows a species picker that's already correctly limited to species with existing posts (per James, this already works as expected). The breed dropdown, once a species is selected, currently shows ALL breeds for that species from the reference table — including breeds that have zero posts. This should behave the same way species already does: only show breeds that actually have at least one post for the currently-selected species.

FIRST: investigate how the species picker currently achieves this correct "only species with posts" behavior — find that query/logic and replicate the same pattern for breeds, scoped one level deeper (species → breed instead of just species). Do not reinvent a different approach if an existing pattern for this exact kind of thing (species) already exists in the codebase.

FIX: When a species is selected in Sniff, the breed dropdown should only list breeds that have at least one post matching that species (respecting whatever "eligible post" rules the existing species logic already applies — e.g. excluding archived/hidden/nursery-only posts if species filtering already excludes those, so the two pickers stay consistent with each other).

ACCEPTANCE TEST: (1) Selecting a species with multiple breeds posted shows only those breeds with actual posts, not the full breed reference list. (2) Selecting a breed from that filtered list and viewing results never produces an empty grid (since only breeds with posts are shown). (3) Switching species updates the breed list accordingly. (4) Species picker behavior is completely unchanged. (5) tsc clean.