SCOPE: Diagnose the live email+password sign-in failure on https://pshpsh.net, then fix the six authentication defects below. One delivery. Nothing outside auth screens and auth routing may change.

CONTEXT: Expo static web export + Express 5 API, Clerk custom flows, Clerk PRODUCTION instance on pshpsh.net. HARD FACTS: the production account's email+password is PROVEN CORRECT — it signs in successfully at the Clerk hosted portal (accounts.pshpsh.net). The SAME credential FAILS on our custom sign-in screen at pshpsh.net, which shows only a generic "could not be completed" error. The real Clerk error is unknown because our screen swallows it.

STEP 0 — EVIDENCE FIRST, BEFORE ANY CODE CHANGES:
Reproduce the failure and capture Clerk's ACTUAL error. Add temporary logging (or use existing logs) around the signIn.create / attemptFirstFactor call to record the full Clerk error object (status, code, longMessage) when a sign-in fails on the custom screen. Paste the raw captured error. State the root cause in one sentence based on that evidence — do not guess. Candidate causes to check against the evidence: an existing session blocking new sign-in ("session already exists"), a signIn flow constructed for the dev instance keys/config, a strategy mismatch, or an unhandled status like needs_second_factor. If the captured error is "session_exists", also verify why — the screen should never have rendered for a signed-in user (that is defect 5).

THEN THE SIX FIXES:

1. WEB SSO CALLBACK. Add a dedicated /sso-callback route that completes the OAuth redirect (Clerk handleRedirectCallback / AuthenticateWithRedirectCallback pattern). All authenticateWithRedirect calls pass redirectUrl to that route and redirectUrlComplete into the app. The SPA fallback must serve this route.

2. SIGN-IN → SIGN-UP OAUTH TRANSFER. A NEW Google user starting on the sign-in screen is transferred into sign-up and lands signed in (transferable verification → signUp.create with transfer: true). Mirror the reverse on the sign-up screen for an EXISTING Google user.

3. FORGOT PASSWORD. Clerk reset_password_email_code strategy: email entry → code + new password → signed in and routed into the app. Entry point: quiet typographic "Forgot password?" link on the sign-in screen — muted text, NO pill/capsule.

4. HONEST INLINE ERRORS. Surface Clerk's real error messages (err.errors[0].longMessage) inline per field/form: wrong password, account not found, session already exists, code incorrect/expired. The generic "could not be completed" message is removed from all auth screens. Pass Clerk's message through; do not invent copy.

5. SESSION-AWARE REDIRECT. /sign-in and /sign-up detect an existing session (isSignedIn) and route straight into the app instead of rendering. No "Session already exists" text may ever appear.

6. COPY. "Don't have an account? Create one" becomes "pshpsh is invite-only." (lowercase pshpsh, always). No capture form yet — that ships with the portal redesign. Keep a working route into sign-up behind existing navigation so testing isn't blocked.

Whatever root cause Step 0 reveals, it must be FIXED in this delivery — if it is not covered by items 1–6, fix it as item 7 and describe the change. Remove any temporary logging before finishing.

DO NOT MODIFY: feed, post detail, reactions, treat, Sniff, Nursery, profile screens, upload pipeline, API routes outside auth, static-file serving and its dotfiles setting, SPA-fallback ordering and exclusions (except serving /sso-callback), fonts, icons, secrets, replit.md. No Reanimated. No new dependencies. Identical behavior web/iOS/Android where the flow exists. All fixes in SOURCE files — the deploy rebuilds from source.

PRESERVATION CLAUSE: must survive intact — email verification on sign-up, session persistence across refresh, sign-out, all non-auth app functionality. Losing any = failed delivery.

ACCEPTANCE — on the LIVE domain pshpsh.net after the user redeploys, evidence pasted, not asserted:
0. THE HEADLINE: email+password sign-in with the existing production account SUCCEEDS on the custom screen in a fresh browser session.
1. Google sign-in with an existing account lands in the app, no bounce.
2. Google sign-in with a brand-new Google account transfers to sign-up and lands in the app.
3. Forgot password end-to-end: email → code → new password → signed in.
4. A wrong password shows Clerk's actual message inline.
5. Visiting /sign-in while signed in routes into the app.
6. GOLDEN PATH: sign in → Home loads → boop persists after refresh → Nursery pager → post flow → sign out.

FINAL REPORT: (1) Step 0's captured Clerk error verbatim and the proven root cause, (2) what changed per item, (3) confirmation everything is in source and committed, (4) explicit statement that the user must press Redeploy before live acceptance can pass.