// Stub for react-native-svg — only reached by FocalImage's error placeholder,
// which this harness never triggers (all images are local data URIs).
export default function Svg() {
  return null;
}
export function Path() {
  return null;
}
