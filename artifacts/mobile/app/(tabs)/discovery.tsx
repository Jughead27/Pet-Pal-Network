/**
 * Sniff Tab route — thin re-export. The screen implementation lives in
 * components/discovery/ (extraction-only split; expo-router treats every
 * file under app/ as a route, so the extracted pieces cannot live here).
 */
export { default } from '@/components/discovery';
